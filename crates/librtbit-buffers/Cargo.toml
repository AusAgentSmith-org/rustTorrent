[package]
name = "librtbit-buffers"
version = "0.1.2"
edition = "2024"
description = "Utils to work with byte buffers in librtbit source code"
license = "MIT"
repository = "https://github.com/AusAgentSmith-org/rustTorrent"

[dependencies]
serde = "1"
serde_derive = "1"
clone_to_owned.workspace = true
bytes = "1"

[lints.clippy]
all = { level = "warn", priority = -1 }

[lints.rust]
unused = "warn"
